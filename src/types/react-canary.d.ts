/// <reference types="react/canary" />

// `<ViewTransition>` ships in the React canary that Next bundles for the App
// Router, but `@types/react` only declares it in its canary entry point, which
// has to be pulled in explicitly.
